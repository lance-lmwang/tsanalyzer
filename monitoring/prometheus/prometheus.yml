global:
  scrape_interval: 2s
  evaluation_interval: 2s

rule_files:
  - "rules.yml"

scrape_configs:
  - job_name: 'tsanalyzer'
    static_configs:
      - targets: ["192.168.1.127:8088"]
        labels:
          instance: 'tsa-appliance'

global:
  scrape_interval: 2s
  evaluation_interval: 2s

rule_files:
  - "rules.yml"

scrape_configs:
  - job_name: 'tsanalyzer'
    static_configs:
      - targets: ['127.0.0.1:8082']
        labels:
          instance: 'tsa-appliance'
